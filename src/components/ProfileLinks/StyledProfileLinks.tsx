import styled from 'styled-components';

export const StyledProfileLinks = styled.div`
  text-align: center;

  a {
    margin: 0 10px;
    text-decoration: none;
  }

  .cv {
    position: relative;
    top: -2px;
  }
`;
